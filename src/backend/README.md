# Backend (FastAPI)

Simple FastAPI backend with a test endpoint.

## Setup

```bash
cd src/backend
pip install -r requirements.txt
```

## Run

```bash
uvicorn main:app --reload
```

API will be at http://127.0.0.1:8000

